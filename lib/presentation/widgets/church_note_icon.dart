import 'package:flutter/material.dart';

class ChurchNoteIcon extends StatelessWidget {
  const ChurchNoteIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'assets/images/icons/church_notes_icon.png',
      width: 80,
      height: 80,
    );
  }
}
